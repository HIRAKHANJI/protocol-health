# v8.10.5 Deep-Dive Audit — Full Findings Register

Six parallel read-only investigations (goal calculator/settings math · projection/calibration ·
data layer · calendar/fast-sessions/schedule · workout engine + plumbing · workout-library
science + AGRO study) ran against v8.10.4 on 2026-08-03. Every finding below was re-verified
against the source before acting. **Status: FIXED (v8.10.5) or DEFERRED (documented here,
untouched in code).** Line numbers reference v8.10.4.

## CRITICAL / HIGH — all FIXED in v8.10.5

| # | Area | Finding | Fix |
|---|---|---|---|
| C1 | Data | Migration auto-backups unrestorable: runner writes raw `ph_*` keys, `_commitBackupRestore` looked up SK logical names — every key silently skipped while the status line claimed success | Restore now maps raw keys via a reverse SK lookup; honest restored/skipped counts; skip note no longer misattributes to "newer app version" |
| C2 | Data | `restoreData` had no undo of any kind — the only destructive full overwrite in the app | Pre-restore snapshot of all current SK data auto-downloads before the commit (best-effort, never blocks a confirmed restore) |
| H1 | Data | Eternal splash: 4 init gates awaited module-ready events forever; one 404/eval-throw = permanent opaque splash, no error surface | `_awaitModuleGate`: 15s timeout per gate → splash removed + actionable alert |
| H2 | Data | Migration-failure alert rendered UNDER the z-9999 splash (alert z-9000) — the failure explanation was invisible | Splash removed before alerting; alert/confirm raised to z-10500 |
| H3 | Calendar | Fast-editor DELETE confirm + validation alert opened BEHIND the z-9100 fast-edit modal — delete flow dead | Same z-10500 raise fixes it (confirm/alert now topmost everywhere) |
| H4 | Calc | `autoFillTDEE` bypassed the TDEE manual-override freeze and silently rewrote stored TDEE on every Settings open / plan-dropdown change | Early-return when `tdeeManualOverride` is set |
| H5 | Calc | Bulk calculator ignored the plan's 2 light days (~60% TDEE) — promised surplus could be a real deficit; maintenance claimed perfect 0 balance | Light-day term added to bulk MODE A/B and maintenance weekly balance; `eatDaysPerWeek = 7 − fast − light` |
| H6 | Calib | `inferActivityMultiplier` 28-day gate mathematically unreachable (window spans max 27) — Phase-9 activity inference permanently dead | Gate now `>= MIN_DAYS − 1` (same off-by-one class as the fixed v7.10.0 calibration gate) |
| H7 | Calib | Zero-tick days (user logged food/weight but no checklist) scored 0% → "low-compliance" exclusion → 3+ days = phantom sickness pattern deferring calibration forever | Low-compliance now requires `done > 0` (user actually touched the checklist) |
| H8 | Engine | Engine toggle + TEMP CUT rendered a fake all-rest week (7 error-days → "Rest") with no fallback — replaced the real program silently | All-error weeks now throw → caught → legacy `workoutContent()` renders |
| H9 | Engine | `plans/bulk.js:179` comma-for-`+`: SAT card's rows shifted into `stretchRows`, `days` lost → card counted into EVERY day's workout totals | Comma → `+`; card args realigned (`'SAT'` day restored) |
| H10 | Calendar | TEMP CUT Fridays: Session D + FLUSH + EVERY DAY cards all pooled into one 23-row "evening" aggregate → e2 auto-tick nearly unreachable Aug 7, guaranteed-failed Aug 14 | FLUSH/Session D now date-gated at render time (`'NONE'` token off-date); EVERY DAY card classified `'daily'`, excluded from session aggregates |

## MEDIUM — FIXED

- **Calc/plumbing:** protein-floor advisory used a fictional 50% split and the *saved* plan — now uses the previewed plan's real `macroSplit.base` protein %, previewed-plan floor (also fixes the MEDIUM stale-plan floor read).
- **Calc:** `calcAdjust` — light-day term added; bulk `Math.abs` sign mask removed (calories below TDEE on a bulk adjust no longer fabricates a gain rate; negative surplus clamps to 0).
- **Engine:** fast-day −20% volume cut was a no-op (`'resistance'` token matched no type set) — now passes `exercise.type`. AGRO fasted sessions get the reduction for the first time.
- **Engine:** `isPlanEligible` fails open — `tempcut` added to the >120 kg block (routes to LITE).
- **Calendar:** fast↔light cross-map unset hole — converting an auto-set fast day to light (or vice versa) now records the unset, so a schedule ADJUST can't recreate a day that is both fast and light.
- **Calendar:** day-modal rest cards emitted an orphan header + unclosed `<div>` (subsequent cards nested inside) — classification now precedes header emission.
- **Content:** creatine mentions in cut/bulk/maintenance/tempcut now carry the §15-mandated "NO loading phase" phrase.
- **Cosmetic-but-fixed:** manage-schedule "Day N of M" now caps at M; `PLAN_ACTIVITY_DEFAULTS.tempcut` added ('1.55' nearest step, true 1.61 documented); aggressive-floor comment 400→300.

## DEFERRED — documented, not fixed (each needs design decisions or is feature work)

**Data layer:** schema-record corruption triggers destructive full re-migration overwriting real fast sessions (needs a 3→4 guard: skip rebuild when `ph_fs_v1` already populated) · restore doesn't clear keys absent from the backup ("overwrite ALL" is false; stale sessions repollute) · older-schema backups restore un-migrated (no post-restore `runMigrations`); pre-v5.1.0 backups never re-migrate · migration 3→4 synthesizes UTC-midnight timestamps → 6→7 can delete manual fast days at |UTC offset|>8h · fresh installs run the full 1→7 chain incl. 2 auto-backup downloads (needs a fresh-install fast-path) · engine-key trio absent from `_SK_EXPECTED_TYPE` · `backupTs` number-vs-string drift · migration layer hardcodes key literals · `downloadJson` can't detect a silently blocked download (`requiresBackup` guarantee is soft) · `reverse()` migrations are dead code (no rollback tooling).

**Calibration/projection:** observed-TDEE intake/weight denominator asymmetry biases TDEE low when unlogged days are high-intake days · compliance modifier double-penalizes the observed rate (and does scale the fast-day deficit despite its comment) · formula-rate 7-day window vs days-to-Sunday horizon mismatch · ATP factor mixes all-time loss over schedule-elapsed days · 0.75 observed-blend cap unreachable (maxes 0.70) · inference cap note hardcodes ±0.2 (module uses ±0.35 for fast plans) · Reality Check hides loss rows when avgIntake=0 (all-fast windows) · loss-sign display convention inverted vs weight direction · corrupt `activityByDayType` renders but isn't computed · projection reads raw fastDays map (not session-aware `isFastDay`) · future-dated weights skew projection (calibration is protected) · DST ms-math in `getSpikeTrimmedWeights` · Sunday shows *next* Sunday's projection unlabeled.

**Calendar/fast:** END TODAY/REMOVE wipe ALL future fast/light days incl. hand-marked ones (needs provenance) · `isFastBroken` is session-global — breaking a multi-day fast on its last morning turns completed middle days orange · live multi-day session's `dates[]` frozen at start date until it ends (middle days invisible mid-fast) · session-awareness inconsistent across surfaces (day modal contradicts itself) · deleting the food entry that broke a fast doesn't un-break it · `editSession` re-homes a past session to today when start cleared · rest-card row ticks silently discarded (visual ✓ never persists) · `updateWorkoutProgress` writes storage in a render path with no dispatch · `removeSchedule` never clears unset maps · `getMostRecentSessionForDate` trusts append order.

**Calc:** `plan.tdee` is dead data — calculator always uses settings TDEE (doc/code divergence; §7's "+ exercise burn × 7" also doesn't exist in code) · `autoFillTDEE` on plan-change computes with the *saved* plan's activity model while previewing another · `calories: 0` storable then read as 1500 via falsy-fallbacks · cleared start-date → NaN day keys (reachable only by manual clearing) · calorie ceiling silently ignored when a deadline is filled (MODE A wins) · projection clamps broken-fast days to 0 but allows negative eating days · hardcoded personal fallbacks (104/91) in shared math · silent sub-1200 TDEE input drop.

**Engine (v9 roadmap items):** completion/deload/progression subsystem inert — `ph_ec_v1` never written, `ph_ell_v1`/`ph_pe_v1` never touched, `daysSinceDeload` hardcoded 0 · progression id-prefix never matches `skill_*`/`chair_*` db ids (`skill_crow-l1-` vs `skillcrow-l1-`) · documented completion shape lacks `targetReps` the math needs · fasted flags hardcoded in templates, ignore real `SK.fastDays` · `applyPushPullCap` never called (weekly guard only; currently no violating week found empirically) · 90-day legacy level lookback vs unbounded engine lookback.

## Science layer (WORKOUTS_LIBRARY.md) — findings for the owner's review (no doc edits made)

- **TEMP CUT is entirely outside the library** (~30 exercises unlisted) — needs a library section or an explicit temporary-plan waiver.
- **Fast-day contradiction:** library says "Fast days prohibit HIIT (AGRO) — only light walking," while agro.js schedules its two hardest fasted conditioning days (Wed/Sat runs + circuits @70-80%). One of the two must change.
- **AGRO prescription-table drift is systemic** (~16 rows contradict agro.js — e.g. "Push-up ladder: N/A" while Evening A leads with it); library also credits AGRO with shadowboxing it doesn't contain.
- **Push:pull actual counts:** AGRO 0.63:1 (pull-dominant only if superman holds count as pulls; true rows = 16 sets/wk) · LITE ≈3.5:1 **violates the ≤1:1 rule as written** · CUT ~1:1 · **BULK 1.20:1 push-dominant — violates its own Rule 09 and "BALANCED" badge** (Mon = 20 push : 0 pull) · MAINT 0.67:1 · TEMP CUT ~0.85:1 (complies best).
- **§2 citation hygiene:** several §2.2/§2.3 sources (Schoenfeld 2016/2017, Helms, NSCA, McGill) are not in CLAUDE.md §15; the "female ±2 reps per ISSN 2018" attribution is invented; Nordic mislabelled L5 (is L6/L7).
- **AGRO supplement stack:** D3+K2 4,000 IU + Osteocare (contains D3 + calcium) likely exceeds the §15 D3 cap and supplemental-calcium cap with no warning — needs formulation verification.
- **AGRO protein floor 1.3 g/kg** is below the app's own cut-plan science (1.6 g/kg, Morton 2017) for its most aggressive deficit — recommend raising.
- **AGRO deficit magnitude** (~1.8 kg/wk at the 1000 ceiling) exceeds the app's own Standard-mode "unrealistic" threshold.
- **Owner's "2,000 kcal in ~3h" burn claim:** honest MET math at ~100 kg → **~1,200–1,700 kcal gross / ~950–1,400 net** for a hard 3-hour mixed session (dash laps + long-break low-rep lifting + foundational calisthenics average ~3.5–6 METs during lifting blocks; 2,000 gross needs ~6.7 METs sustained for 3h = nonstop jogging). Preworkout adds ~3–8% (~40–130 kcal); EPOC ~6–15% post-session. The app's internal per-session accounting (~650–1,000 + 130/burst) is the credible model.

## Verified-correct highlights (for confidence)

Manual backup covers all 22 SK keys incl. engine trio · checksum round-trip sound · IDB auto-restore can't clobber fresh data · MODE A/B cut math matches spec exactly · risk-flag thresholds match §7 · Mifflin-St Jeor textbook-correct · all six plans' DOW arrays match their counts · schedule day count = calculator output (no fencepost) · 7700 constant consistent everywhere · calendar month/leap/DST-safe date handling · 16h rule correctly marks tempcut's Wed single + weekend double · sw.js cache list ⇄ disk fully consistent both directions · no native dialogs, no toISOString date keys, no SK bypasses in app code · engine rep/set modulator math correct with audit trail · push:pull weekly guard held empirically across all templated plans × presets.
